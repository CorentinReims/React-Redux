const addAction = {
  type: "ADD"
};

const removeAction = {
  type: "REMOVE"
};

const add10Action = {
  type: "ADD10"
};

const remove10Action = {
  type: "REMOVE10"
};

const resetAction = {
  type: "RESET"
};

export default (resetAction,
remove10Action,
add10Action,
removeAction,
addAction);
